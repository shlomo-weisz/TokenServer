/**
 * JWT creation, validation, and request authentication.
 *
 * <p>The server uses stateless Bearer tokens. The security package authenticates
 * requests early and leaves higher-level authorization checks to the services.
 */
package com.tokenlearn.server.security;
